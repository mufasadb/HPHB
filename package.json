{
  "name": "hphb",
  "version": "1.0.0",
  "description": "attempt at recreating Harry Potter a Hogwarts Battle",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "cd client && npm install",
    "server": "nodemon server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Daniel Beach",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.1",
    "concurrently": "^4.1.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
